% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/orders.R
\name{create_order}
\alias{create_order}
\title{Create Order}
\usage{
create_order(
  ticker,
  side,
  action,
  count,
  type = "limit",
  yes_price = NULL,
  no_price = NULL,
  expiration_ts = NULL,
  sell_position_floor = NULL,
  buy_max_cost = NULL,
  kalshi_access_token = "KALSHI_API",
  demo = FALSE
)
}
\arguments{
\item{ticker}{Character. The market ticker.}

\item{side}{Character. Order side: "yes" or "no".}

\item{action}{Character. Order action: "buy" or "sell".}

\item{count}{Integer. Number of contracts.}

\item{type}{Character. Order type: "limit" or "market". Defaults to "limit".}

\item{yes_price}{Integer. Price in cents (1-99) for yes contracts. Required for limit orders.}

\item{no_price}{Integer. Price in cents (1-99) for no contracts. Alternative to yes_price.}

\item{expiration_ts}{Integer. Unix timestamp for order expiration. Optional.}

\item{sell_position_floor}{Integer. Minimum position to maintain when selling. Optional.}

\item{buy_max_cost}{Integer. Maximum cost in cents for buy orders. Optional.}

\item{kalshi_access_token}{Character. Environment variable name for API key.}

\item{demo}{Logical. Use demo environment if TRUE.}
}
\value{
A list with order details including order_id.
}
\description{
Creates a new order in a market. Orders can be limit or market orders,
for either yes or no contracts.
}
\details{
Places a real, authenticated order by sending a POST request to
`/trade-api/v2/portfolio/orders`. The order body is assembled from `ticker`,
`side`, `action`, `count`, and `type`, with `yes_price`/`no_price`,
`expiration_ts`, `sell_position_floor`, and `buy_max_cost` added only when
supplied. Limit orders require either `yes_price` or `no_price`, and `side`,
`action`, and `type` are validated before the request is sent. On success the
new order's id is reported and the order object is returned.
}
\examples{
\dontrun{
# Buy 10 yes contracts at 45 cents
order <- create_order(
  ticker = "KXHIGHNY-25FEB08-B54",
  side = "yes",
  action = "buy",
  count = 10,
  yes_price = 45
)

# Sell 5 no contracts at 60 cents
order <- create_order(
  ticker = "KXHIGHNY-25FEB08-B54",
  side = "no",
  action = "sell",
  count = 5,
  no_price = 60
)
}
}
\seealso{
Other trading:
\code{\link[=amend_order]{amend_order()}},
\code{\link[=batch_cancel_orders]{batch_cancel_orders()}},
\code{\link[=batch_create_orders]{batch_create_orders()}},
\code{\link[=cancel_all_orders]{cancel_all_orders()}},
\code{\link[=cancel_order]{cancel_order()}},
\code{\link[=decrease_order]{decrease_order()}},
\code{\link[=get_order]{get_order()}}
}
\concept{trading}
