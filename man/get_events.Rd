% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/events.R
\name{get_events}
\alias{get_events}
\title{Get Events}
\usage{
get_events(
  status = NULL,
  series_ticker = NULL,
  with_nested_markets = FALSE,
  limit = 100,
  max_pages = NULL,
  kalshi_access_token = "KALSHI_API",
  demo = FALSE
)
}
\arguments{
\item{status}{Character. Filter by status: "open", "closed", "settled".}

\item{series_ticker}{Character. Filter by series ticker.}

\item{with_nested_markets}{Logical. Include nested market data.}

\item{limit}{Integer. Results per page (1-200). Defaults to 100.}

\item{max_pages}{Integer. Maximum pages to fetch. NULL for all.}

\item{kalshi_access_token}{Character. Environment variable name for API key.}

\item{demo}{Logical. Use demo environment if TRUE.}
}
\value{
A tibble with event information.
}
\description{
Retrieves a list of events with optional filtering. An event is a specific
occurrence within a series (e.g., "NYC High Temperature on Feb 8, 2025").
}
\details{
Pages through the `/trade-api/v2/events` endpoint via [kalshi_paginate()],
passing `status`, `series_ticker`, and `limit` (1-200, default 100) as query
parameters, and adding `with_nested_markets=true` only when requested. Each
event is flattened into a row containing its event ticker, series ticker, title,
subtitle, category, status, `mutually_exclusive` flag, and strike date; the
nested market data itself is not unpacked into the tibble. Use `max_pages` to
limit how many pages are fetched, and expect an empty tibble plus a warning when
no events match.
}
\examples{
\dontrun{
# Get all open events
events <- get_events(status = "open")

# Get events for a specific series
events <- get_events(series_ticker = "KXHIGHNY")
}
}
\seealso{
Other events:
\code{\link[=build_event_tickers]{build_event_tickers()}},
\code{\link[=get_event]{get_event()}},
\code{\link[=get_event_markets]{get_event_markets()}}
}
\concept{events}
