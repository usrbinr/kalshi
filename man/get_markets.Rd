% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markets.R
\name{get_markets}
\alias{get_markets}
\title{Get Markets}
\usage{
get_markets(
  status = NULL,
  series_ticker = NULL,
  event_ticker = NULL,
  tickers = NULL,
  limit = 100,
  max_pages = NULL,
  kalshi_access_token = "KALSHI_API",
  demo = FALSE
)
}
\arguments{
\item{status}{Character. Filter by status: "unopened", "open", "closed", "settled".}

\item{series_ticker}{Character. Filter by series ticker.}

\item{event_ticker}{Character. Filter by event ticker.}

\item{tickers}{Character vector. Specific market tickers to retrieve.}

\item{limit}{Integer. Results per page (1-1000). Defaults to 100.}

\item{max_pages}{Integer. Maximum pages to fetch. NULL for all. Defaults to NULL.}

\item{kalshi_access_token}{Character. Environment variable name for API key.}

\item{demo}{Logical. Use demo environment if TRUE.}
}
\value{
A tibble with market information including ticker, title, status,
  yes_price, no_price, volume, and other market details.
}
\description{
Retrieves a list of markets with optional filtering. Handles pagination
automatically to return all matching results.
}
\details{
Queries the \code{/trade-api/v2/markets} endpoint, passing \code{status},
\code{series_ticker}, \code{event_ticker}, \code{tickers} (collapsed into a
comma-separated string), and \code{limit} as query parameters. Results are
fetched page by page via cursor-based pagination until exhausted or
\code{max_pages} is reached, and each market is flattened into one row of a
tibble containing identifiers, status, bid/ask and last prices, volumes,
open interest, result, and close/expiration times. An empty tibble is
returned (with a warning) when no markets match.
}
\examples{
\dontrun{
# Get all open markets
markets <- get_markets(status = "open")

# Get markets for a specific series
markets <- get_markets(series_ticker = "KXHIGHNY")
}
}
\seealso{
Other markets:
\code{\link[=get_candlesticks]{get_candlesticks()}},
\code{\link[=get_market]{get_market()}},
\code{\link[=get_orderbook]{get_orderbook()}},
\code{\link[=get_trades]{get_trades()}}
}
\concept{markets}
