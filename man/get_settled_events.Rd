% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/history.R
\name{get_settled_events}
\alias{get_settled_events}
\title{Get Settled Events}
\usage{
get_settled_events(
  series_ticker = NULL,
  limit = 100,
  max_pages = NULL,
  kalshi_access_token = "KALSHI_API",
  demo = FALSE
)
}
\arguments{
\item{series_ticker}{Character. Filter by series ticker.}

\item{limit}{Integer. Results per page (1-200). Defaults to 100.}

\item{max_pages}{Integer. Maximum pages to fetch. NULL for all.}

\item{kalshi_access_token}{Character. Environment variable name for API key.}

\item{demo}{Logical. Use demo environment if TRUE.}
}
\value{
A tibble with settled event information.
}
\description{
Retrieves settled events with optional filtering.
}
\details{
A thin wrapper around [get_events()] that hard-codes `status = "settled"`,
so it returns only events that have already resolved. The `series_ticker`,
`limit`, `max_pages`, and authentication arguments are passed straight
through, and pagination continues until all settled events are retrieved or
`max_pages` is reached.
}
\examples{
\dontrun{
# Get all settled events for a series
events <- get_settled_events(series_ticker = "KXHIGHNY")
}
}
\seealso{
Other history:
\code{\link[=get_event_candlesticks]{get_event_candlesticks()}},
\code{\link[=get_event_results]{get_event_results()}},
\code{\link[=get_market_history]{get_market_history()}},
\code{\link[=get_series_history]{get_series_history()}},
\code{\link[=get_settled_markets]{get_settled_markets()}},
\code{\link[=get_trade_history]{get_trade_history()}}
}
\concept{history}
