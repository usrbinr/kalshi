% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/history.R
\name{get_settled_markets}
\alias{get_settled_markets}
\title{Get Settled Markets}
\usage{
get_settled_markets(
  series_ticker = NULL,
  event_ticker = NULL,
  from = NULL,
  to = NULL,
  limit = 200,
  max_pages = NULL,
  kalshi_access_token = "KALSHI_API",
  demo = FALSE
)
}
\arguments{
\item{series_ticker}{Character. Filter by series ticker.}

\item{event_ticker}{Character. Filter by event ticker.}

\item{from}{Date, character, or integer. Start of date range.
Accepts "2026-01-01", as.Date(), or Unix seconds.}

\item{to}{Date, character, or integer. End of date range.
Accepts "2026-02-07", as.Date(), or Unix seconds.}

\item{limit}{Integer. Results per page (1-1000). Defaults to 200.}

\item{max_pages}{Integer. Maximum pages to fetch. NULL for all.}

\item{kalshi_access_token}{Character. Environment variable name for API key.}

\item{demo}{Logical. Use demo environment if TRUE.}
}
\value{
A tibble with settled market information including results.
}
\description{
Retrieves settled markets with optional date filtering. Useful for
analyzing historical outcomes.
}
\details{
Queries the `/trade-api/v2/markets` endpoint with `status = "settled"` and
paginates through all matching pages (or up to `max_pages`). The `from` and
`to` arguments are converted to Unix-second timestamps and sent as the
`min_settled_ts` and `max_settled_ts` filters, so the date range applies to
each market's settlement time. The returned tibble flattens each market into
one row with its ticker identifiers, title, settlement `result`, volume,
open interest, and the close, expiration, and settlement times.
}
\examples{
\dontrun{
# Get all settled markets for a series
results <- get_settled_markets(series_ticker = "KXHIGHNY")

# Get markets settled in a date range
results <- get_settled_markets(
  series_ticker = "KXHIGHNY",
  from = "2026-01-01",
  to = "2026-02-01"
)

# Get markets settled in the last 7 days
results <- get_settled_markets(from = Sys.Date() - 7)
}
}
\seealso{
Other history:
\code{\link[=get_event_candlesticks]{get_event_candlesticks()}},
\code{\link[=get_event_results]{get_event_results()}},
\code{\link[=get_market_history]{get_market_history()}},
\code{\link[=get_series_history]{get_series_history()}},
\code{\link[=get_settled_events]{get_settled_events()}},
\code{\link[=get_trade_history]{get_trade_history()}}
}
\concept{history}
