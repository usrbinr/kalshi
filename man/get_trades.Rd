% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markets.R
\name{get_trades}
\alias{get_trades}
\title{Get Market Trades}
\usage{
get_trades(
  ticker = NULL,
  limit = 100,
  min_ts = NULL,
  max_ts = NULL,
  max_pages = 1,
  demo = FALSE
)
}
\arguments{
\item{ticker}{Character. Optional market ticker to filter trades.}

\item{limit}{Integer. Number of trades to return (1-1000). Defaults to 100.}

\item{min_ts}{Integer. Minimum timestamp (Unix ms) filter.}

\item{max_ts}{Integer. Maximum timestamp (Unix ms) filter.}

\item{max_pages}{Integer. Maximum pages to fetch. NULL for all.}

\item{demo}{Logical. Use demo environment if TRUE.}
}
\value{
A tibble with trade information.
}
\description{
Retrieves recent trades for all markets or a specific market.
}
\details{
Queries the \code{/trade-api/v2/markets/trades} endpoint with \code{ticker},
\code{limit}, and the \code{min_ts}/\code{max_ts} Unix-millisecond timestamp
filters as query parameters. Results are gathered via cursor-based
pagination, but \code{max_pages} defaults to 1 so only the first page is
fetched unless raised. Each trade is shaped into one row of a tibble with
ticker, trade_id, count, yes/no price, taker_side, and created_time; an empty
tibble is returned when there are no matching trades.
}
\examples{
\dontrun{
# Get recent trades
trades <- get_trades(limit = 50)

# Get trades for specific market
trades <- get_trades(ticker = "KXHIGHNY-25FEB08-B54")
}
}
\seealso{
Other markets:
\code{\link[=get_candlesticks]{get_candlesticks()}},
\code{\link[=get_market]{get_market()}},
\code{\link[=get_markets]{get_markets()}},
\code{\link[=get_orderbook]{get_orderbook()}}
}
\concept{markets}
